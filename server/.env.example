# Copy to .env and adjust.
PORT=5000

# Local MongoDB (or `docker compose up -d` from the mern-app folder):
MONGODB_URI=mongodb://127.0.0.1:27017/rolodeck

# MongoDB Atlas example:
# MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/rolodeck?retryWrites=true&w=majority
