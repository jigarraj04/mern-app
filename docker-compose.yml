# Optional: run a local MongoDB with `docker compose up -d`
services:
  mongo:
    image: mongo:7
    container_name: rolodeck-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
